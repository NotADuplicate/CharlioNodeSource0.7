/// @descriptio Damage and bleed
if(global.teamNum[other.num] != global.teamNum[num] || other.num == num) {
	bleedNum = other.num
	if(global.invincibility = 0 && spellShield == 0) {
		/*with(ball_game) {
			node_send(buffer,"eventName","Status","Target",ball_player.num,"Status Num", 53)
		}*/
		bleed = 10;
	}
}
