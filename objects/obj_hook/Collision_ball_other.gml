/// @description Insert description here
// You can write your code in this editor
/*if(other.spellShield) 
	instance_destroy();
else if(other.num != num && hooked == 0) {
	hooked = other;
	alarm[0] = 60;
}