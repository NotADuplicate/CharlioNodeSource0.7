function scr_standoff_others() {
	        with(standoff_other) {
	            if(num == argument[0]) {
	                    x = argument[1];
	                    y = argument[2];
						image_angle = argument[3];
						pointing = argument[4];
	                }
	                //else
	                  //  instance_destroy();
	            }



}
